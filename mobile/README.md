# MIZAN3G Mobile

This Flutter client is intentionally thin: it calls the Laravel API and never computes SKB/IKG or stores AI-provider keys. Install Flutter/Dart, then run `flutter pub get` and `flutter run --dart-define=MIZAN3G_API_URL=http://<host>:8001`.
